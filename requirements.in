# IMPORTANT NOTE ABOUT DEPENDENCY CONFLICT:
# The original Sesame CSM requires moshi==0.2.2 which needs torch<2.7
# But the latest Jetson-optimized wheels are for torch==2.7.0
# We provide two options below - choose ONE approach by uncommenting:

# OPTION 1: Use latest Jetson wheels (torch 2.7.0)
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0
moshi @ git+https://github.com/kyutai-labs/moshi.git@main#subdirectory=moshi

# OPTION 2: Use slightly older torch compatible with moshi
# torch==2.2.0
# torchvision==0.17.0
# torchaudio==2.2.0
# moshi==0.2.2   # Required by original Sesame CSM

# Remaining dependencies - common to both approaches
# Core ML Framework - Aligning with Jetson Index (JP6/cu128) 
# VQ library - Version that works with torch>=2.0
vector_quantize_pytorch>=1.22.15

# Tokenization library - Flexible version
tokenizers>=0.13.3
# Transformer models - Flexible version
transformers>=4.31.0
# Hugging Face Hub interaction - Flexible version
huggingface_hub>=0.16.4
# Training utilities - Flexible version
accelerate>=0.25.0

# Audio file handling - Flexible version
soundfile>=0.12.1
# Audio manipulation - Flexible version
pydub>=0.25.1
# Audio device interaction (may not be strictly needed in container) - Flexible version
sounddevice>=0.5.0

# Ebook parsing - Flexible version
ebooklib>=0.18.0
# HTML/XML parsing - Flexible version
beautifulsoup4>=4.12.2
# PDF handling - Flexible version
PyPDF2>=3.0.1
# PDF text extraction - Flexible version
pdfminer.six>=20221105
# Natural language processing toolkit (for sentence splitting) - Flexible version
nltk>=3.8.1
# Audio processing library (dependency for silentcipher examples) - Flexible version
librosa>=0.9.0

# Tensor operations library - Version >=0.8.0 as per analysis recommendation
einops>=0.8.0

# PyTorch Acceleration & Optimization libraries - NOTE: Check compatibility
torchao==0.1.0
torchtune==0.3.0

# Watermarking/Encryption - Currently disabled
# silentcipher>=1.0.1 # May require additional testing

# Triton support - Check compatibility with torch 2.7
tritonclient[all]==2.55.0  # Aligns with Triton Server v2.55.0
